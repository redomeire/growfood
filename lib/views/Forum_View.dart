import 'package:flutter/material.dart';
import 'package:growfood/theme.dart';

class Forum_View extends StatelessWidget {
  const Forum_View({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: green2,
        title: const Text("Forum_View"),
      ),
      body: const Center(
        child: Text("UnderConstruction"),
      ),
    );
  }
}
